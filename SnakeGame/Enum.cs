using System;

namespace SnakeGame
{
    internal class Enum
    {
        public enum PointType
        {
            Free,
            Wall,
            Fruit,
            Snake
        }

        public enum Direction
        {
            Up,
            Down,
            Left,
            Right
        }
    }
}
