namespace SnakeGame
{
    internal class Enum
    {
        internal enum PointType
        {
            Free,
            Wall,
            Fruit,
            Snake
        }

        internal enum Direction
        {
            Up,
            Down,
            Left,
            Right
        }

        internal enum Tab
        {
            Play,
            About,
            Exit,
        }
    }
}
